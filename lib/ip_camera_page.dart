import 'package:flutter/material.dart';

class IpCameraPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('IP Camera Page'),
      ),
      body: Center(
        child: Text('IP Camera page content goes here'),
      ),
    );
  }
}
