import 'package:flutter/material.dart';

class StopToCountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stop to count'),
      ),
      body: Center(
        child: Text('IP Camera page content goes here'),
      ),
    );
  }
}
