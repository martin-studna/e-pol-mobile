import 'package:flutter/material.dart';

class DashboardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Phone Camera Page'),
      ),
      body: Center(
        child: Text('Phone Camera page content goes here'),
      ),
    );
  }
}
